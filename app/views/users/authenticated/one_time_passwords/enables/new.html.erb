<% content_for :title do %>Enable Two Factor<% end %>

<% content_for :header do %>
    <h1>Enable Two Factor</h1>
<% end %>

<% content_for :breadcrumbs do %>
    <li><%= link_to 'Two Factor Authentication', [:users, :one_time_passwords, :statuses] %></li>
    <li><%= link_to 'Enables', [:new, :users, :one_time_passwords, :enables] %></li>
<% end %>

<div class="row gutter30">
    <div class="col-md-5">
        <div class="block qr-code text-center">
            <%= @enable_form.qr_code %>
            <p class="secret"><code><%= @enable_form.otp_secret %></code></p>
        </div>
    </div>



    <div class="col-md-7">
        <div class="block qr-code">
            <p>Two-factor authentication greatly improves your account security by requiring you to enter a second password that's generated by Google Authenticator on your smartphone.</p>
            <p>The passwords generated by the two factor process are valid for only 90 seconds, so if an attacker does happen to capture your password with a keylogger or otherwise they're unlikely to be able to gain access to your account.</p>
            <p>We highly recommend all users enable this feature, no matter if you're a business or a hobbyist!</p>

            <hr>

            <%= simple_form_for @enable_form, url: [:users, :one_time_passwords, :enables], html: { class: 'form-box form-horizontal' } do |f| %>
                <%= f.input :otp_secret, as: :hidden %>
                <%= f.input :otp %>

                <div class="form-group">
                    <div class="col-md-5 col-md-offset-2">
                        <%= f.button :submit, 'Enable Two Factor', data: { disable_with: 'Enabling...' }, id: 'enable_form_submit', class: 'btn btn-primary' %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>


